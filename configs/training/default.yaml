# Training settings
epochs: 20
learning_rate: 0.001
weight_decay: 0.0001
optimizer: "adam"  # Options: "sgd", "adam"
scheduler:
  type: "StepLR"
  step_size: 5
  gamma: 0.1
early_stopping:
  patience: 5
  min_delta: 0.001
